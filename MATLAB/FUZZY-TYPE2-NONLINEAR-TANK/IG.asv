function ig = IG(H,a1,a2,a3,u,r,y),


e1 = sum(abs(u))%/size(u,1)

var_u = u'-e1;

e2 = (var_u*var_u')%/size(var_u,2)

desvio_total = r-y';
e3 = sum(abs(desvio_total))/size(desvio_total,2)


ig = a1*e1+a2*e2+a3*e3;

end 